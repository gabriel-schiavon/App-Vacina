version: '3'
services:
  backend:
    build: ./api-vacinas
    ports:
      - "3000:3000"
    networks:
      - vacinas-net

  frontend:
    build: ./VaxiAgenda
    ports:
      - "8100:8100"
    networks:
      - vacinas-net
    depends_on:
      - backend

networks:
  vacinas-net:
